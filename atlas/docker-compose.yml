services:
  server-web:
    image: nginx
    ports:
      - 8000:80
      - 9000:80
    volumes:
      - ./labo10/app:/usr/share/nginx/html:ro
      - ./TP3_GEO7630/app1:/usr/share/nginx/html:ro
    restart: always